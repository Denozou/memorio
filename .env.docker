# Docker Compose environment (local/dev)

# Database Configuration (targets the postgres service in docker-compose)
SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/memorio
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=postgres

# JWT Configuration
JWT_SECRET=change-me-super-secret-at-least-32-bytes-long-123456
JWT_ISSUER=memorio
JWT_ACCESS_TOKEN_MINUTES=60
JWT_REFRESH_TOKEN_MINUTES=10080

# OAuth2 (optional for local testing; keep dummy values so Spring placeholder resolution doesn't fail)
GOOGLE_CLIENT_ID=dummy
GOOGLE_CLIENT_SECRET=dummy
GOOGLE_REDIRECT_URI=http://localhost:8080/login/oauth2/code/google
FACEBOOK_CLIENT_ID=dummy
FACEBOOK_CLIENT_SECRET=dummy
FACEBOOK_REDIRECT_URI=http://localhost:8080/login/oauth2/code/facebook

# CORS
FRONTEND_URL=http://localhost:5173

# Frontend API URL (for Docker builds - points to local backend)
VITE_API_URL=http://localhost:8080

# Email (disable for local Docker)
EMAIL_ENABLED=false
MAIL_USERNAME=
MAIL_PASSWORD=

# Encryption key (base64-encoded, decodes to 32 bytes for AES-256)
ENCRYPTION_KEY=PEfiux+57P8WBw5FLTzdMBtdAdf2+Dj+wIlVBo1horE=

# Redis (matches compose defaults)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=90dGr9T385u7pJ0YhV8LZcfD0PSGpV9bdvRn
